# Local Data Directory

This directory stores local-first runtime artifacts for development.

- `app.sqlite`: SQLite metadata database created by backend migrations.
- `uploads/`: source content selected for analysis.
- `previews/`: generated preview artifacts for source evidence.

Large source files should remain out of git.
